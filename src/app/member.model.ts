export class Member {
  // it would be great to get this info from an API
  constructor (public name: string, public instrument: string, public primary: boolean, public img: string, public id: number) { }
}
